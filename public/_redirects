# Intentionally empty. Render processes _redirects BEFORE checking static files,
# so a catch-all rewrite here would override our prerendered per-route HTML.
# Instead, prerender.mjs writes 404.html (the SPA shell) for Render's built-in
# "not found" fallback. Since every public route is prerendered, any URL that
# doesn't match a static file is a genuine 404.
