# robots.txt for https://getonramp.io
# Allow all crawlers full access to public content.

User-agent: *
Allow: /

# Exclude the Open Graph image template (helper file, not a real page)
Disallow: /og-image.html

# Sitemap
Sitemap: https://getonramp.io/sitemap.xml

# LLM-friendly site summary (https://llmstxt.org)
# Note: "Llms-Txt:" is not a standard robots.txt directive — Google's validator
# flags it as an error. AI crawlers discover /llms.txt via the well-known path
# convention (llmstxt.org), so no directive is needed here.
# See: https://getonramp.io/llms.txt
